# castanet
A short but sweet script that scans a user-specified amount of random IPs for open ports (ports pre-chosen by me) and places them in a file with some nice formatting.

Split between a python script and a bash script.
